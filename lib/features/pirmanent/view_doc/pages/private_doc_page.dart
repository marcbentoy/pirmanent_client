import 'package:flutter/material.dart';

class PrivateDocsPage extends StatelessWidget {
  const PrivateDocsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Private Docs Page"),
    );
  }
}
