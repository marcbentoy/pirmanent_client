import 'package:flutter/material.dart';

class PublicDocsPage extends StatelessWidget {
  const PublicDocsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Public Docs Page"),
    );
  }
}
