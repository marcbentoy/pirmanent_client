import 'package:flutter/material.dart';

class UploadDocPage extends StatelessWidget {
  const UploadDocPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Upload Doc Page"),
    );
  }
}
