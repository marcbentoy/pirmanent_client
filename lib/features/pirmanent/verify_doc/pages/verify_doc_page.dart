import 'package:flutter/material.dart';

class VerifyDocPage extends StatelessWidget {
  const VerifyDocPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Verify Doc Page"),
    );
  }
}
