import 'package:flutter/material.dart';

class SignDocPage extends StatelessWidget {
  const SignDocPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Sign Doc Page"),
    );
  }
}
